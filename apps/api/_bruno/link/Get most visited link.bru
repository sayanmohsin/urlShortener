meta {
  name: Get most visited link
  type: http
  seq: 5
}

get {
  url: {{host}}/api/links/most-visited
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  : 
}
